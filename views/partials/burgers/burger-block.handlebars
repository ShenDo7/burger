<div>
	{{this.burger_name}}
	{{#unless this.devoured}}{{else}}<button class="change-devour btn-small btn-flat" data-id="{{this.id}}" data-newdevoured="{{this.devoured}}">Devour Now</button>{{/unless}}
</div>
